# Docker Environment Configuration
# These variables are used when running services in Docker containers

# Dashboard Configuration (Docker)
VITE_DASHBOARD_PORT=3050
VITE_HOST=0.0.0.0
# When running in Docker, use the service name (orchestrator) instead of localhost
VITE_API_URL=http://orchestrator:3051

# Orchestrator Configuration
ORCHESTRATOR_PORT=3051

# Database Configuration (Docker networking)
# Use 'postgres' service name when in Docker, not localhost
DATABASE_URL=postgresql://agentic:agentic_dev@postgres:5432/agentic_sdlc

# Redis/Message Bus Configuration (Docker networking)
# Use 'redis' service name when in Docker, not localhost
REDIS_URL=redis://redis:6379

# API Keys
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
