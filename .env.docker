# ============================================================================
# Docker Environment Configuration
# These variables are used when running services in Docker containers
# Note: Use service names (postgres, redis, orchestrator) instead of localhost
# ============================================================================

# ============================================================================
# Service Ports (REQUIRED - will fail if not set)
# ============================================================================

# Dashboard: React development server and web UI (REQUIRED)
VITE_DASHBOARD_PORT=3050
VITE_HOST=0.0.0.0

# Orchestrator: Main API server and workflow engine (REQUIRED)
# When running in Docker, uses service name 'orchestrator' for internal communication
ORCHESTRATOR_PORT=3000

# Analytics Service: Metrics and analytics API (REQUIRED)
ANALYTICS_SERVICE_PORT=3001

# ============================================================================
# Database Configuration (Docker networking - REQUIRED)
# ============================================================================
# Use 'postgres' service name for Docker container networking
DATABASE_URL=postgresql://agentic:agentic_dev@postgres:5432/agentic_sdlc

# ============================================================================
# Redis/Message Bus Configuration (Docker networking - REQUIRED)
# ============================================================================
# Use 'redis' service name for Docker container networking
REDIS_URL=redis://redis:6379
REDIS_HOST=redis
REDIS_PORT=6379

# ============================================================================
# API Configuration
# ============================================================================
# Dashboard API URL - uses service name 'orchestrator' for Docker networking
VITE_API_URL=http://orchestrator:3000

# ============================================================================
# API Keys (REQUIRED)
# ============================================================================
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

# ============================================================================
# Logging & Environment
# ============================================================================
NODE_ENV=production
LOG_LEVEL=info
HOST=0.0.0.0
