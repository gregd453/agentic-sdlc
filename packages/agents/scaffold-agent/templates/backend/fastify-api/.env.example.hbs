# Environment Configuration
# Copy this file to .env and update values

# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info
API_VERSION=1.0.0

# Database Configuration
# Each app has its own isolated PostgreSQL database (Zyp policy)
DATABASE_URL="postgresql://{{name}}_user:{{name}}_password@localhost:5432/{{name}}_db"

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Security
# Note: NO JWT_SECRET here - apps don't sign JWTs (Zyp policy)
# Shell-BFF handles all JWT signing