{
  "name": "@agentic-sdlc/config-manager",
  "version": "1.0.0",
  "description": "Agent configuration management with validation and override hierarchy",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@agentic-sdlc/shared-types": "workspace:*",
    "zod": "^3.25.76",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.0",
    "@vitest/coverage-v8": "^1.4.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  }
}
